#include <stdio.h>

int main(void)
{
    printf("laura32 firmware skeleton\n");

    // In an embedded context, this function would not normally return.
    // For now, just return 0 so this is also runnable on a host system.
    return 0;
}
